This project is based on Ionic and Capacitor.

using `socket.io-client` 4.5.1

This code will try to connect to `socket.io` server (ver: 4.5.1) with dummy account.

The connection works on Android devices but not on iOS devices.

---------

To run this project:

- npm i
- npm run sync
- npm run android
- npm run ios